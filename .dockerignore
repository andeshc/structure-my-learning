# Dependencies — reinstalled inside the image
node_modules
client/node_modules
server/node_modules

# Client build output — rebuilt inside the image
client/dist

# Runtime data — provided via Docker volumes
data/
server/public/generated/

# Dev / test artifacts
server/data/
*.test.db
coverage/

# Secrets and local config
.env
.env.*
!.env.example

# Repo metadata
.git
.gitignore
docs/

# Editor and OS
.DS_Store
*.log
